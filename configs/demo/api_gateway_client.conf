# Demo api gateway client config
service.name=api_gateway_client
runtime.environment=dev
host=0.0.0.0
port=${API_GATEWAY_PORT:-8080}
client.host=${CLIENT_API_HOST:-127.0.0.1}
client.timeout_ms=${CLIENT_TIMEOUT_MS:-3000}
