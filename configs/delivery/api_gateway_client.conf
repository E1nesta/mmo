# Delivery api gateway client config
service.name=api_gateway_client
runtime.environment=staging
host=0.0.0.0
port=${API_GATEWAY_PORT:-8080}
client.host=${CLIENT_API_HOST:-nginx}
client.timeout_ms=${CLIENT_TIMEOUT_MS:-3000}
